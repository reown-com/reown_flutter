name: pos_client
description: "A Flutter SDK that enables Point of Sale (POS) systems to accept cryptocurrency payments through WalletConnect network"
version: 0.0.3
homepage: https://github.com/reown-com/reown_flutter
repository: https://github.com/reown-com/reown_flutter/tree/master/packages/pos_client

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=1.10.0"

dependencies:
  collection: ^1.19.1
  event: ^3.1.0
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  http: ^1.5.0
  json_annotation: ^4.9.0
  qr_flutter_wc: ^0.0.3
  reown_core: ^1.3.8
  reown_sign: ^1.3.9

dev_dependencies:
  build_runner: ^2.4.13
  build_version: ^2.1.1
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  freezed: ^3.2.0
  json_serializable: ^6.10.0
  logger: ^2.5.0
  mockito: ^5.4.4

flutter:
  uses-material-design: true
