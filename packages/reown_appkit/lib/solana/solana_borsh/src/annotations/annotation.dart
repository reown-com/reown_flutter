/// Annotation
/// ------------------------------------------------------------------------------------------------
library;

/// An interface for Borsh annotations.
abstract class BorshAnnotation<T> {
  /// Creates an annotation for [T] data types.
  const BorshAnnotation();
}
