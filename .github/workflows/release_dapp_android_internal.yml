name: Android Dapp (internal) deploy

on:
  workflow_dispatch:
  # pull_request:
  #   types:
  #     - closed

# inputs:
# working-directory
# flavor
# BUNDLE_ID

jobs:
  build:
    # if: (github.event.pull_request.merged == true && github.event.pull_request.base.ref == 'develop') || github.event_name == 'workflow_dispatch'
    runs-on: macos-14

    steps:
      # Checkout the repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # Set up environment
      - uses: ./.github/actions/dependencies
        with:
          platform: 'android'

      # Setup Firebase CLI with service account
      - name: Setup Firebase
        run: |
          npm install -g firebase-tools@13.0.1
          echo '${{ secrets.GSA_KEY }}' > ${{ runner.temp }}/gcp_key.json
          chmod 600 ${{ runner.temp }}/gcp_key.json
          echo "GOOGLE_APPLICATION_CREDENTIALS=${{ runner.temp }}/gcp_key.json" >> $GITHUB_ENV

      # Add Secrets file on dapp
      - name: Add Secrets file
        working-directory: packages/reown_appkit/example/base/android
        run: touch secrets.properties && echo "${{ secrets.SECRETS_FILE }}" >> secrets.properties

      # Add Keystore file on dapp
      - name: Add Keystore file
        working-directory: packages/reown_appkit/example/base/android
        run: echo "${{ secrets.KEYSTORE }}" | base64 --decode >> app/debug.keystore

      # Fastlane
      - name: Fastlane
        id: fastlane
        working-directory: packages/reown_appkit/example/base/android
        env:
          FLAVOR: "internal"
          PROJECT_NAME: "AppKit Flutter"
          PROJECT_ID: ${{ secrets.DAPP_PROJECT_ID }}
          DAPP_SENTRY_DSN: ${{ secrets.DAPP_SENTRY_DSN }}
          FIREBASE_DAPP_ID: ${{ secrets.FIREBASE_DAPP_ID_INTERNAL }}
          DOWNLOAD_URL: ${{ secrets.DOWNLOAD_URL_DAPP_INTERNAL }}
        run: |
          PUBSPEC_FILE=../pubspec.yaml
          FILE_VALUE=$(grep "^version: " $PUBSPEC_FILE)
          PARTS=(${FILE_VALUE//:/ })
          FULL_VERSION=${PARTS[1]}
          VERSION_NUMBER=${FULL_VERSION%%-*}

          fastlane add_plugin firebase_app_distribution
          fastlane release_firebase project_id:$PROJECT_ID app_version:$VERSION_NUMBER

      # Send Slack notification
      - name: Send Slack notification
        if: always() && !cancelled()
        uses: slackapi/slack-github-action@v2.1.0
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: |
            {
              "text": "Android Build Report - ${{ env.PROJECT_NAME }} - ${{ env.FLAVOR }}",
              "blocks": [
                {
                  "type": "header",
                  "text": { "type": "plain_text", "text": "🤖 Android Build Report" }
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Project:*\n`${{ env.PROJECT_NAME }}`" },
                    { "type": "mrkdwn", "text": "*Release Type:*\n`${{ env.FLAVOR }}`" },
                    { "type": "mrkdwn", "text": "*Branch:*\n`${{ github.ref_name }}`" }
                  ]
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Build & Firebase:*\n`${{ steps.fastlane.outcome == 'success' && '✅ Success' || '❌ Failed' }}`" },
                    { "type": "mrkdwn", "text": "*Overall Status:*\n`${{ job.status == 'success' && '✅ Success' || '❌ Failed' }}`" }
                  ]
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": { "type": "plain_text", "text": "View Workflow Run" },
                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    }${{ steps.fastlane.outcome == 'success' && env.DOWNLOAD_URL != '' && format(',{{"type":"button","text":{{"type":"plain_text","text":"View in Firebase"}},"url":"{0}"}}', env.DOWNLOAD_URL) || '' }}
                  ]
                }
              ]
            }
        env:
          PROJECT_NAME: "AppKit Flutter"
          FLAVOR: "internal"
          DOWNLOAD_URL: ${{ secrets.DOWNLOAD_URL_DAPP_INTERNAL }}

# Launch locally
# Remove build: if:
# act -j build --container-architecture linux/amd64 -P macos-14=-self-hosted --secret-file .github/workflows/.env.secret -W .github/workflows/release_dapp_android_internal.yml
# act --container-architecture linux/amd64 -P macos-14=-self-hosted --secret-file .github/workflows/.env.secret -W .github/workflows/release_dapp_android_internal.yml workflow_dispatch
