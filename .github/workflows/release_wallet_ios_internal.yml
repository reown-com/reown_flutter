name: iOS Wallet (internal) deploy

on:
  workflow_dispatch:
  # pull_request:
  #   types:
  #     - closed

# inputs:
# working-directory
# flavor
# BUNDLE_ID

jobs:
  build:
    # if: (github.event.pull_request.merged == true && github.event.pull_request.base.ref == 'develop') || github.event_name == 'workflow_dispatch'
    runs-on: macos-26

    steps:
      # Checkout the repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # Setup SSH for Match certificates repository
      - name: Setup SSH for Match
        uses: webfactory/ssh-agent@dc588b651fe13675774614f8e6a936a468676387 # v0.9.0
        with:
          ssh-private-key: ${{ secrets.MATCH_SSH_KEY }}

      # Cache
      - name: Cache
        uses: actions/cache@v3
        with:
          path: |
            .build
            SourcePackagesCache
            DerivedDataCache
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-spm-

      # Install Flutter and Dependencies
      - uses: ./.github/actions/dependencies
        with:
            platform: 'ios'

      # Build App
      - name: Build App
        working-directory: packages/reown_walletkit/example
        env:
          PROJECT_ID: ${{ secrets.WALLET_PROJECT_ID }}
          WALLET_SENTRY_DSN: ${{ secrets.WALLET_SENTRY_DSN }}
          WCP_API_KEY: ${{ secrets.WCP_API_KEY }}
        run: |
          PUBSPEC_FILE=pubspec.yaml
          FILE_VALUE=$(grep "^version: " $PUBSPEC_FILE)
          PARTS=(${FILE_VALUE//:/ })
          FULL_VERSION=${PARTS[1]}
          VERSION_NUMBER=${FULL_VERSION%%-*}

          flutter build ios --flavor internal --build-name $VERSION_NUMBER --dart-define="PROJECT_ID=$PROJECT_ID" --dart-define="WALLET_SENTRY_DSN=$WALLET_SENTRY_DSN" --dart-define="WCP_API_KEY=$WCP_API_KEY" --config-only --release

      - name: Set Xcode Version
        run: sudo xcode-select -s /Applications/Xcode_26.1.app

      # Fastlane
      - name: Fastlane
        id: fastlane
        working-directory: packages/reown_walletkit/example/ios
        env:
          FLAVOR: "internal"
          PROJECT_NAME: "WalletKit Flutter"
          BUNDLE_ID: "com.walletconnect.flutterwallet.internal"
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_DAPP_ID: ${{ secrets.APPLE_WALLET_ID_INTERNAL }}
          PROJECT_ID: ${{ secrets.WALLET_PROJECT_ID }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          APP_STORE_KEY_ID: ${{ secrets.APP_STORE_KEY_ID }}
          APPLE_ISSUER_ID: ${{ secrets.APPLE_ISSUER_ID }}
          APP_STORE_CONNECT_KEY: ${{ secrets.APP_STORE_CONNECT_KEY }}
          MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}
          TESTFLIGHT_URL: ${{ secrets.TESTFLIGHT_URL_WALLET_INTERNAL }}
        run: |
          PUBSPEC_FILE=../pubspec.yaml
          FILE_VALUE=$(grep "^version: " $PUBSPEC_FILE)
          PARTS=(${FILE_VALUE//:/ })
          FULL_VERSION=${PARTS[1]}
          VERSION_NUMBER=${FULL_VERSION%%-*}

          fastlane release_testflight username:$APPLE_ID project_id:$PROJECT_ID app_id:$APPLE_DAPP_ID app_store_key_id:$APP_STORE_KEY_ID apple_issuer_id:$APPLE_ISSUER_ID match_git_url:$MATCH_GIT_URL app_version:$VERSION_NUMBER flavor:internal

      # Send Slack notification
      - name: Send Slack notification
        if: always() && !cancelled()
        uses: slackapi/slack-github-action@v2.1.0
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: |
            {
              "text": "iOS Build Report - ${{ env.PROJECT_NAME }} - ${{ env.FLAVOR }}",
              "blocks": [
                {
                  "type": "header",
                  "text": { "type": "plain_text", "text": "🍎 iOS Build Report" }
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Project:*\n`${{ env.PROJECT_NAME }}`" },
                    { "type": "mrkdwn", "text": "*Release Type:*\n`${{ env.FLAVOR }}`" },
                    { "type": "mrkdwn", "text": "*Branch:*\n`${{ github.ref_name }}`" }
                  ]
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Build & TestFlight:*\n`${{ steps.fastlane.outcome == 'success' && '✅ Success' || '❌ Failed' }}`" },
                    { "type": "mrkdwn", "text": "*Overall Status:*\n`${{ job.status == 'success' && '✅ Success' || '❌ Failed' }}`" }
                  ]
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": { "type": "plain_text", "text": "View Workflow Run" },
                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    }${{ steps.fastlane.outcome == 'success' && env.TESTFLIGHT_URL != '' && format(',{{"type":"button","text":{{"type":"plain_text","text":"View in TestFlight"}},"url":"{0}"}}', env.TESTFLIGHT_URL) || '' }}
                  ]
                }
              ]
            }
        env:
          PROJECT_NAME: "WalletKit Flutter"
          FLAVOR: "internal"
          TESTFLIGHT_URL: ${{ secrets.TESTFLIGHT_URL_WALLET_INTERNAL }}

# Launch locally
# act -j build --container-architecture linux/amd64 -P macos-26=-self-hosted --secret-file .github/workflows/.env.secret -W .github/workflows/release_wallet_ios_internal.yml
# act --container-architecture linux/amd64 -P macos-26=-self-hosted --secret-file .github/workflows/.env.secret -W .github/workflows/release_wallet_ios_internal.yml workflow_dispatch
